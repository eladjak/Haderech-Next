/**
 * Global configuration for API routes
 * Sets all API routes as dynamic by default
 */

export const dynamic = "force-dynamic";
export const fetchCache = "force-no-store";
export const revalidate = 0;
